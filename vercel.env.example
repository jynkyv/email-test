# Vercel 部署环境变量模板
# 请将这些变量添加到 Vercel Dashboard 的环境变量中

# Google OAuth2 配置
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=https://your-domain.vercel.app/auth/callback
GOOGLE_REFRESH_TOKEN=your_refresh_token
GOOGLE_EMAIL=your_email@gmail.com

# Supabase 配置
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# 应用配置
NEXT_PUBLIC_BASE_URL=https://your-domain.vercel.app

# 部署说明：
# 1. 将 your-domain.vercel.app 替换为您的实际 Vercel 域名
# 2. 确保所有 Google OAuth2 配置都已正确设置
# 3. 确保 Supabase 项目已创建并配置
# 4. 在 Google Cloud Console 中添加您的 Vercel 域名到授权重定向 URI 